# Copy this file to .env.local (gitignored) and set your own ../server/
# deploy's URL:
#
#   cd client-react
#   copy .env.example .env.local      (Windows)
#   cp .env.example .env.local          (macOS/Linux)
#
# Then edit the copy, not this file. Why it is a build input: README.md.
VITE_API_BASE=https://your-api-worker.your-subdomain.workers.dev
