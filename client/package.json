{
  "name": "job-search-tracker-client",
  "private": true,
  "description": "Tracker webpage (static assets on a Cloudflare Worker, no build step). Talks to ../server/'s API cross-origin. See README.md.",
  "scripts": {
    "predeploy": "node predeploy-check.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
